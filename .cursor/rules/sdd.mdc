---
alwaysApply: true
---

# SDD：规范驱动开发（Monorepo）

在本仓库实现功能、改 API/WebSocket 行为、或切换 Nest/Python 火山对接方式时：

1. **先读** `docs/sdd/INDEX.md`，确认是否已有相关 **SPEC** / **ADR**。
2. **再改代码**。若行为变化：同步更新 `docs/sdd/specs/` 下对应文档（或新建 SPEC），重大架构变动补充 `docs/sdd/adr/`。
3. **跨包变更**（Nest、Python、`apps/anime-video-generate-agent-client`、legacy Next）必须在同一条 SPEC 里写清 **契约与验收**，参见 `docs/sdd/packages.md`。
4. **不要**仅凭 README 或口头约定改契约；README 面向入门，**SSOT** 在 `docs/sdd/`。

模板：`docs/sdd/templates/feature-spec.md`、`docs/sdd/templates/adr.md`。
