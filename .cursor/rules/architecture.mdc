---
alwaysApply: true
---

# AI Video Platform Architecture

> 基于火山引擎 Seedance（方舟内容生成）的 AI 短视频平台架构设计  
> 面向：
>
> - AI 视频生成
> - AI Agent
> - 长任务系统
> - 多模态工作流
> - Timeline 编辑器
> - SaaS 平台化
>
> 核心思想：
>
> ```text
> AI Video Operating System
> ```
>
> 而不是：
>
> ```text
> Text To Video Demo
> ```

---

# 1. Overall Architecture

```text
┌─────────────────────────────────────────────┐
│                  Frontend                   │
│                                             │
│  React + Tailwind + Zustand（可 Next 宿主） │
│                                             │
│  - AI Chat UI                               │
│  - Timeline Editor                          │
│  - Storyboard                               │
│  - Asset Management                         │
│  - Streaming UI                             │
│  - Agent Visualization                      │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│              BFF / API Gateway               │
│                                             │
│                  NestJS                     │
│                                             │
│  - REST / GraphQL                           │
│  - WebSocket Gateway                        │
│  - Streaming（SSE 等）                       │
│  - Upload / Auth Session                    │
│  - 直呼火山方舟 HTTP（与 cURL 同契约）       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│           Workflow & Domain Layer           │
│                                             │
│                  NestJS                     │
│                                             │
│  - Workflow Engine                          │
│  - Agent Orchestration                      │
│  - Queue Producers                          │
│  - Billing                                  │
│  - Retry / Scheduling                       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│                Queue System                 │
│                                             │
│           BullMQ + Redis Streams            │
│                                             │
│  - Retry                                    │
│  - Priority Queue                           │
│  - Delayed Jobs                             │
│  - Progress Tracking                        │
│  - Dead Letter Queue                        │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│             AI Workflow Engine              │
│                                             │
│  - Prompt Engine                            │
│  - Storyboard Engine                        │
│  - Director Agent                           │
│  - Character Consistency                    │
│  - Scene Planner                            │
│  - Timeline Planner                         │
│  - Retry Planner                            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│              AI Generation Layer            │
│                                             │
│  - Seedance（方舟 REST：tasks 创建/查询/删除）│
│  - LLM APIs                                 │
│  - Voice APIs                               │
│  - Subtitle APIs                            │
│  - Embedding Models                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│               Media Pipeline                │
│                                             │
│  - FFmpeg                                   │
│  - Stitching                                │
│  - Audio Mixing                             │
│  - Subtitle Burn                            │
│  - Export                                   │
│  - Compression                              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│              Storage & Delivery             │
│                                             │
│  - S3 / R2                                  │
│  - CDN                                      │
│  - Preview Cache                            │
│  - Scene Cache                              │
│  - Embedding Cache                          │
└─────────────────────────────────────────────┘
```

**Monorepo 约定（目录）**

- `services/anime-video-generate-agent-ai`（Python）：**默认不动**；不因接入 Nest 而改写其行为。对接方式仍是 **HTTP 网关（FastAPI）**、stdin 子进程或日后 Nest **直呼方舟**，与 Python **同一上游契约**即可。
- **新增 NestJS**：落在 **`services/anime-video-generate-agent-nest`**（与 **`services/anime-video-generate-agent-ai`** 并列），专门做 Agent / 编排 / HTTP 网关；**不要**塞进 `apps/anime-video-generate-agent-server`，避免与 Next 网关职责混在一起。
- **过渡期**：Next（`apps/anime-video-generate-agent-server`）只做 **页面 + Socket.io + 既有 `/api`**；演进方向是 **`anime-video-generate-agent-nest` 承接 BFF 与编排**，Next 逐步变薄。
- **默认本地开发**：根目录 **`pnpm dev`** 仅 **Nest + Vite**；Nest **直呼火山 REST**，前端 **`/api` + Socket.io** 指向 Nest。**`pnpm dev -- --legacy`** 才启用 Python 网关 + Next 旧栈。

---

# 2. Why This Architecture

核心原因：

AI 视频平台不是：

```text
CRUD
```

而是：

```text
Long Running AI Runtime
```

平台核心问题：

- AI 不确定性
- 长任务
- Streaming
- 多模态
- Agent Workflow
- 视频状态同步
- 可恢复性
- 高成本

所以：

必须：

```text
前后端分层
+ Workflow分层
+ Queue化
+ 状态机化
```

---

# 3. Frontend Architecture

# 技术栈

```text
React（本仓库：Vite；亦可 Next 仅作前端宿主）
Tailwind
Zustand
TanStack Query
Framer Motion
Remotion
React Flow
Lexical
```

---

# 目录结构

```text
src/
├── app/
├── features/
│
├── entities/
│   ├── task/
│   ├── video/
│   ├── scene/
│   ├── agent/
│   └── timeline/
│
├── workflows/
│   ├── generation/
│   ├── storyboard/
│   └── export/
│
├── agents/
│   ├── director-agent/
│   ├── prompt-agent/
│   └── repair-agent/
│
├── timeline/
├── websocket/
├── streaming/
├── ai/
├── shared/
└── lib/
```

---

# 4. Why Next.js（仅作可选前端宿主）

**BFF 与业务 Runtime 在 NestJS**；Next.js 若使用，应主要承担：

- 前端路由与页面（App Router / RSC 等）
- 与 NestJS 之间的 **浏览器端调用**（不在 Next Route Handler 里长期堆业务）

禁止将 Next 描述为**唯一**或**长期** BFF；过渡期 `server.ts` 聚合网关是技术债，规划迁出。

---

# 5. Why NestJS

NestJS：

负责：

```text
业务 Runtime
```

包括：

- Workflow
- Queue
- Agent
- FFmpeg
- Billing
- WebSocket
- Retry
- Scheduling

原因：

AI 平台本质：

```text
Distributed Workflow System
```

---

# 6. Why TanStack Query

AI 平台：

核心是：

```text
Server State Synchronization
```

不是：

```text
UI State
```

TanStack Query 负责：

- Polling
- Retry
- Cache
- Deduplication
- Optimistic Update
- Reconnect

---

# 7. AI Workflow System

# 工作流

```text
User Prompt
    ↓
Prompt Enhancement
    ↓
Director Agent
    ↓
Storyboard Planning
    ↓
Scene Planning
    ↓
Seedance Generation
    ↓
Retry / Repair
    ↓
FFmpeg Stitching
    ↓
Subtitle / Audio
    ↓
Export
```

---

# 8. Core AI Modules

---

## Prompt Engine

负责：

- Prompt增强
- 风格统一
- 镜头语言
- 模板系统
- Prompt DSL

---

## Director Agent

负责：

- 剧情理解
- 镜头规划
- 时长控制
- 情绪推进
- Retry决策

---

## Storyboard Engine

负责：

- 自动拆镜头
- Scene连续性
- 多镜头规划
- Timeline生成

---

## Character Consistency

负责：

- 人物一致性
- Face Lock
- Outfit Persistence
- Embedding Injection

---

# 9. Queue System

# 为什么必须 Queue

AI 视频：

```text
5~20分钟
```

不能同步。

必须：

```text
异步任务化
```

---

# 标准流程

```text
Client Submit
    ↓
NestJS Create Job
    ↓
BullMQ Queue
    ↓
Worker Consume
    ↓
Seedance Generate
    ↓
FFmpeg Stitch
    ↓
Upload CDN
    ↓
WebSocket Notify
```

---

# 10. Timeline System

# Timeline 才是平台核心

不是：

```text
视频生成
```

而是：

```text
AI Video Editing
```

---

# Timeline 支持：

- 多轨道
- 音频
- 字幕
- AI Replace
- AI Extend
- AI Regenerate
- Transition
- Keyframe

---

# 技术方案

```text
React
+
Remotion
+
Canvas
+
FFmpeg
```

---

# 11. Streaming Architecture

# AI 平台必须 Streaming First

支持：

- token streaming
- partial JSON
- tool streaming
- workflow streaming
- progress streaming

---

# 状态机

```text
THINKING
PLANNING
GENERATING
RETRYING
STITCHING
EXPORTING
COMPLETED
FAILED
```

禁止：

```ts
loading = true
```

---

# 12. Storage Architecture

# 必须分层缓存

```text
Prompt Cache
Scene Cache
Video Cache
Embedding Cache
Preview Cache
```

---

# 生成层级

```text
draft
preview
hd
export
```

禁止：

```text
直接生成高清
```

---

# 13. Agent Visualization

用户必须知道：

AI 在干嘛。

必须：

```text
[分析剧情]
[拆分镜头]
[生成角色]
[生成场景]
[调用Seedance]
[拼接视频]
```

否则：

用户会认为：

```text
系统卡死
```

---

# 14. Core Engineering Challenges

---

## AI Non-determinism

AI 输出不稳定。

必须：

- zod validation
- parser repair
- retry pipeline
- schema enforcement

---

## Long Running Tasks

必须解决：

- reconnect
- resume
- persistence
- websocket recovery

---

## Streaming Complexity

必须支持：

- partial update
- rollback
- optimistic UI
- incremental rendering

---

## Timeline Complexity

最难模块：

- 拖拽
- 缩放
- 轨道同步
- 音视频同步
- 虚拟化
- 回放同步

---

## Agent Debugging

问题来源可能：

- Prompt
- Memory
- Tool
- Queue
- Workflow
- Parsing
- Model

必须：

```text
可观测
```

---

# 15. Cursor Engineering Rules

# Core Philosophy

本项目不是：

```text
调用AI接口
```

而是：

```text
AI Runtime Platform
```

---

# Frontend Rules

---

## 所有 AI 状态必须状态机化

正确：

```ts
enum TaskStatus {
  THINKING,
  PLANNING,
  GENERATING,
  STITCHING,
  COMPLETED,
  FAILED
}
```

禁止：

```ts
loading = true
```

---

## 所有 AI 输出必须 validate

正确：

```ts
schema.parse(response)
```

禁止：

```ts
response.scene
```

---

## 所有长任务必须支持恢复

必须：

- reconnect
- resume
- retry
- websocket recovery

---

## Streaming First

必须：

- optimistic UI
- partial rendering
- progressive rendering

---

# Agent Rules

---

## Prompt 禁止硬编码

必须：

- DSL化
- Template化
- 可组合

---

## Agent 必须可观测

必须输出：

- thinking
- planning
- tool_call
- retry
- error

---

## Tool Call 必须幂等

因为：

Agent 会 retry。

---

# Video Rules

---

## 所有视频生成必须异步

必须：

```text
Queue + Worker
```

---

## 视频必须分层生成

```text
draft
preview
hd
export
```

---

## 所有媒体必须缓存

必须：

- prompt cache
- embedding cache
- scene cache

---

# Performance Rules

---

## Timeline 必须虚拟化

因为：

```text
timeline node > 10000
```

---

## 禁止一次性加载大视频

必须：

- chunk
- lazy load
- stream

---

# Architecture Rules

---

## 栈与边界 / 火山引擎

- **Python 包**：`services/anime-video-generate-agent-ai` **非必要不改动**；Nest / Agent 增量放在 **`services/anime-video-generate-agent-nest`**。
- **视频生成**：走方舟 **`POST/GET/DELETE .../contents/generations/tasks`**（或文档当前路径），在 NestJS 内用 `fetch`/HTTP 客户端封装，**与官方 cURL 一致**，便于排障。
- **请求体**：`content[].type=text` 的 `text` **仅放纯提示词**；`resolution`、`ratio`、`duration`、`camera_fixed`、`seed` 等用**文档顶层字段**；勿把参数缩写成非官方字符串塞进 `text`。
- **SDK 与 HTTP**：若用 Ark SDK，**不得**传入该 SDK 未支持的字段（如部分版本不接受的 `fps`）；以 SDK 与文档为准，必要时只走 HTTP。
- **schema**：对 LLM 与对上游 API 的 payload 均做 **validate / repair**，再发出网络请求。

---

## 所有模块事件驱动

推荐：

- Event Bus
- Message Queue

---

## Workflow 必须持久化

因为：

AI任务：

```text
可能运行10分钟以上
```

---

## 所有任务必须可重放

必须支持：

- replay
- retry
- audit
- debug

---

# Final Philosophy

真正的平台壁垒不是：

```text
模型API
```

而是：

```text
Workflow
Timeline
Agent
Consistency
Streaming
State Machine
Cost Optimization
```